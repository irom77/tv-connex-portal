'use strict';

/**
 * Module dependencies.
 */
var errorHandler = require('./errors.server.controller'),
    _ = require('lodash');

/**
 * List of Servers
 */
exports.connect = function(req, res) {
    res.json({});
};

